Function:

reverse every k nodes of a linked list.
If the list size is not a multiple of k, then leave the remainder nodes as is
 
Example:

Inputs:  1->2->3->4->5->6->7->8->NULL and k = 3

Output:  3->2->1->6->5->4->7->8->NULL

Comment:

1. all Node are Integer by default. So didn't test other type of Node
2. BubbleSort.java is a Sort algorithm，it also can use other Sort algorithm
3. GenerateNode.java is used to generate Node
4. ReverseNode.java is used to implement the function
5. Didn't add comment in Code since there is no complex logic.
6. all test case in ReverseNodeTest are pass in my local env. run ReverseNodeTest in junit